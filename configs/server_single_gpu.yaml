seed: 0
epochs: 300
patience: 40
learning_rate: 0.001
weight_decay: 0.0001
hidden_dim: 64
topo_dim: 64
num_layers: 2
backbone: hgnn
fusion: gated
topology_encoder: deepset
complex_kind: relbs
device: cuda
