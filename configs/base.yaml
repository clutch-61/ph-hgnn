seed: 0
epochs: 30
patience: 8
learning_rate: 0.001
weight_decay: 0.0001
hidden_dim: 32
topo_dim: 32
num_layers: 2
backbone: hgnn
fusion: gated
topology_encoder: deepset
complex_kind: scc
device: cpu
